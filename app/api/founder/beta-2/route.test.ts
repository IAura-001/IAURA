import{describe,expect,it,vi}from"vitest";const mocks=vi.hoisted(()=>({dashboard:vi.fn()}));
vi.mock("@/core/beta2/server",async()=>{const actual=await vi.importActual<typeof import("@/core/beta2/server")>("@/core/beta2/server");return{...actual,getBeta2Dashboard:mocks.dashboard}});import{GET}from"./route";import{Beta2FounderAccessError}from"@/core/beta2/server";
describe("founder Beta 2 API",()=>{it("does not expose cross-user data to a normal user",async()=>{mocks.dashboard.mockRejectedValueOnce(new Beta2FounderAccessError());expect((await GET()).status).toBe(403);});it("returns founder-authorized aggregate data",async()=>{mocks.dashboard.mockResolvedValueOnce({participants:[],summary:{invited:0}});const response=await GET();expect(response.status).toBe(200);expect(await response.json()).toMatchObject({summary:{invited:0}});});});

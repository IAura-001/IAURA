import {describe,expect,it} from "vitest";import {normalizeBetaFeedback} from "../feedback";
describe("Beta feedback privacy boundary",()=>{it("accepts allowlisted category and intentional text separately from analytics",()=>expect(normalizeBetaFeedback({kind:"support",category:"bug",text:"The upload button failed",answers:{current_tool:"Figma"}})).toEqual(expect.objectContaining({kind:"support",category:"bug",text:"The upload button failed"})));
it("rejects unknown categories and empty submissions",()=>{expect(normalizeBetaFeedback({kind:"support",category:"critical",text:"x"})).toBeNull();expect(normalizeBetaFeedback({kind:"contextual",category:"valuable"})).toBeNull();});
it("bounds text and allowlists structured answer keys",()=>{const value=normalizeBetaFeedback({kind:"exit_survey",category:"other",text:"x".repeat(5000),answers:{willingness_to_pay:"YES",Prompt:"secret"}})!;expect(value.text).toHaveLength(4000);expect(value.answers).toEqual({willingness_to_pay:"YES"});});});

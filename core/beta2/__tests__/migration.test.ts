import{readFileSync}from"node:fs";import{resolve}from"node:path";import{describe,expect,it}from"vitest";
const sql=readFileSync(resolve(process.cwd(),"supabase/migrations/20260830200000_measured_beta2_readiness_v1.sql"),"utf8");
describe("Beta 2 schema authority",()=>{it("requires founder authorization for assignment and reporting",()=>{expect(sql.match(/is_active_founder\(auth\.uid\(\)\)/g)?.length).toBeGreaterThanOrEqual(2);expect(sql).toContain("Founder access required");});it("links existing invites to cohorts and joins only after authoritative claim",()=>{expect(sql).toContain("alter table public.beta_invites add column cohort_id");expect(sql).toContain("beta_membership_join_cohort");});it("cascades participant, feedback, and notes on account deletion",()=>expect(sql.match(/references auth\.users\(id\) on delete cascade/g)?.length).toBeGreaterThanOrEqual(4));it("keeps feedback text in a separate protected table",()=>{expect(sql).toContain("create table public.beta_feedback");expect(sql).not.toMatch(/beta_usage_events[\s\S]{0,100}feedback_text/);});it("keeps the cohort draft so this mission cannot enable invitations",()=>expect(sql).toContain("'beta_2','Measured Beta 2','draft'"));});
